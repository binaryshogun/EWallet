<Window x:Class="_119_Karpovich.DisplayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_119_Karpovich" xmlns:viewmodel="clr-namespace:_119_Karpovich.ViewModel"
        mc:Ignorable="d"
        Title="E-Wallet" MinHeight="450" MinWidth="800"
        WindowStartupLocation="CenterScreen" WindowState="Minimized" >
    <Window.DataContext>
        <viewmodel:DisplayViewModel/>
    </Window.DataContext>
    <Grid>
        <Frame x:Name="MainFrame" Grid.Row="1"/>

        <Grid Grid.Row="2">
            <TextBlock Text="{Binding TimeNow}" Grid.Column="2"/>

            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
        </Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="45"/>
            <RowDefinition/>
            <RowDefinition Height="45"/>
        </Grid.RowDefinitions>
    </Grid>
</Window>
